# Part (h) - regex to find two consecutive repeated words (with possible punctuation between)
# Owner: Member 3

import re
from common import load_book

# TODO: write a regular expression that matches two consecutive repeated words,
#       allowing punctuation between them (e.g. "well well", "so so", "no, no").
#       hints: \b is a word boundary, and \1 references the first captured group.
pattern = r""  # TODO

text = load_book()

# TODO: find all matches in the text and print them.
# TODO (for the report): add the regex and the matches you found.
